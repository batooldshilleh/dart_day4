import 'dart:math';

void main(List<String> args) {
  int a = 9;
  int b = 70;
  int c = min(a, b);
  print(c);

  //***************************

  double minemum = min(9, 20);
  //we can bass number

  print(minemum.toInt());
  //print the number as integer

  print(max(a, b));
  //maxemum number

  double d = sqrt(9);
}
